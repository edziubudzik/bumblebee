%h1 Nowa strona

= error_messages_for :page

- form_for [@project, @page] do |f|
	= render :partial => 'form', :locals => { :f => f }
	
	%p
		%input{ :type => "submit", :value => "Utwórz" }/
		lub 
		%a= link_to 'anuluj', project_path(@project)