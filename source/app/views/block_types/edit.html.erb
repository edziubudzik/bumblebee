<h1><%= @block_type.name %></h1>

<%= error_messages_for :block_type %>

<% form_for @block_type do |f| %>
	<%= render :partial => 'form', :locals => { :f => f, :block_type => @block_type } %>

	<p>
          <input type="submit" value="Zapisz" /> lub <%= link_to 'anuluj', block_type_path(@block_type) %>
	</p>
<% end %>
