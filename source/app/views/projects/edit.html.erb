<h1><%= @project.name %></h1>

<%= error_messages_for :project %>

<% form_for @project do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
	
	<p>
		<input type="submit" value="Zapisz" /> lub <%= link_to 'anuluj', project_path(@project) %>
	</p>
<% end %>